Main.cpp:
    Add command line for MIDI stuff
    --help
    check argument ranges

GPFunctions.h:

GPExperiment.cpp:
    Pass time-varying variables to evaluate()
    change "200" to params->waveFileBufferSize
    use parms->fitnessFunctionType

GPNetwork.cpp:
    Add constructor to parse a network from a string
    
GPSynth.cpp:
    make initial population all unique (equals for GPNetwork and GPNode)
    Fill in selectFromEvaluated() skeleton code including greedy over
    Implement other types of reproduction
    do TODOs
    make a map from network ID to GPNetwork*
    remove fitness containers
    change evaluated and upForEvaluation to sets for out of order evaluation/multiply fitness assignments
    
GPNode.h:

FunctionNode.cpp:
    change mutationcode to account for types of arity functions

All node subclasses:
    mutate()
    equals()
